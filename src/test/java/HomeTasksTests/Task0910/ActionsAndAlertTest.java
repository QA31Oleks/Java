package HomeTasksTests.Task0910;

//import org.example.Task0910.ActionsAndAlert;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.openqa.selenium.Alert;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

/*public class ActionsAndAlertTest {
    // метод перевіряє наявність тексту повідомлення у "alert: clicked: quit"
    @Test
    public void isAretsTextEquals(){
        WebDriver webDriver = new ChromeDriver();
        WebDriver.TargetLocator targetLocator = webDriver.switchTo();
        ActionsAndAlert actionsAndAlert = new ActionsAndAlert();
        actionsAndAlert.contexClickAndQuitClick(webDriver);
        Alert alert = targetLocator.alert();
        Assertions.assertEquals("clicked: quit", alert.getText());
        webDriver.quit();
    }
}*/
